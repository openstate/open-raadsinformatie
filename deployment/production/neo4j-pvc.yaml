apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neodata
  namespace: production
spec:
  storageClassName: ssd
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 60Gi
